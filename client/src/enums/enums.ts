export enum UserType {
	COMPANY = "company",
	RESTAURANT = "restaurant",
	EMPLOYEE = "employee",
}

export enum OrderStatus {
	PENDING = "PENDING",
	COMPLETED = "COMPLETED",
	CANCELLED = "CANCELLED",
}
